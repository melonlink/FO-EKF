# Literature workspace

`manifest.csv` is the source of truth for references used in the feasibility review. PDFs are downloaded to `papers/` only when an authoritative open-access source is available. The PDFs remain local and are ignored by Git; this repository tracks the citation, source URL, access status, and checksum instead.

The manifest and download helper are added on `dev-codex` after the literature review validates the sources.

