# Field template only: this file is not auto-loaded. Export these variables in
# the process environment. Shared source data remain read-only.
ECG_DATA_DIR=../PUBLIC/data_PTB-XL

# Project-owned generated roots; workflow-specific subdirectories are added.
ECG_PROCESSED_DIR=./data/processed
ECG_OUTPUT_DIR=./artifacts
